package Ventanas;

import Reproductor.Reproductor;
import javafx.scene.layout.BorderPane;
import javafx.stage.Stage;

public class GUI_Reproductor {
    protected void start(Stage primaryStage){
        /*
        BorderPane ventana = new BorderPane();
        Scene scene = new Scene(ventana, 920,300);
        primaryStage.setTitle("Registro!");
        primaryStage.setScene(scene);
        primaryStage.show();

         */
    }//start

    //Reproductor reproductor = new Reproductor();
    //botonrep ->   reproductor.Reproducir("Fondo.mp3");


}//fin clase GUI_Reproductor
